import React, {Component} from "react";
import Portal from "./components/inventoryPortal"

export default class Inventory extends Component {
    render() {
        return(
            <Portal/>
        );
    }
}
